<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CalenderMapper">

	<select id="getCalenderList" resultType="com.kh.love.calender.vo.CalenderVo">
	SELECT 
    C.NO
    ,C.CODE
    ,B.CHECK_DATE
    ,T.TITLE
    ,T.CONTENT
    ,C.DEL_YN
	FROM CALENDER C
	JOIN TODO T ON C.CODE = T.CODE
	JOIN SCHEDULE S ON T.CODE = S.CODE
	JOIN ANNIVERSARY A ON C.NO = A.NO
	JOIN ACCOUNT_BOOK B ON C.NO = B.NO
	WHERE C.CODE = #{code}
	AND C.DEL_YN =  'N'
	</select>

</mapper>